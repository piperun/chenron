name: cache_manager
description: A caching package for handling metadata and image caching with persistence
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.4
  cached_network_image: ^3.4.1
  path_provider: ^2.1.5
  logging: ^1.2.0

  flutter_cache_manager: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
