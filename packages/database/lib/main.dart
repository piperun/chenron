/// Deprecated alias for `package:database/database.dart`.
///
/// `database.dart` is the canonical public entry point. This file is
/// kept as a forwarding shim so existing imports continue to work; new
/// code should import `package:database/database.dart` directly.
library;

export "package:database/database.dart";
