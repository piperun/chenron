name: database
description: Database schema and operations for Chenron.
version: 0.6.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.26.0
  drift_flutter: ^0.2.0
  sqlite3: ^2.7.5
  path_provider: ^2.1.4
  basedir: any
  platform_provider: any
  core: any
  app_logger: any
  cron: ^0.6.1

  # Add other dependencies as needed based on analysis
  # cuid2, collection, etc. might be needed if used in database files
  cuid2: ^3.1.0
  collection: any
  meta: ^1.12.0
  freezed_annotation: ^3.0.0
  get_it: ^8.0.0
  intl: ^0.20.2
  path: ^1.9.0
  web_archiver: ^0.1.0
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.20.0
  build_runner: ^2.4.15
  freezed: ^3.0.0-0.0.dev
  chenron_mockups: any

