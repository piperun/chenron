name: web_archiver
description: "Archive.org client and helpers for Chenron, extracted as a reusable package."
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  app_logger: any
  http: ^1.2.2
  path_provider: ^2.1.4
  platform_provider: any
  html: ^0.15.4

dev_dependencies:
  database: any
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  chenron_mockups: any
  drift: ^2.28.2
