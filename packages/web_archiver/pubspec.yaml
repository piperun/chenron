name: web_archiver
description: "Archive.org client and helpers for Chenron, extracted as a reusable package."
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  http: ^1.2.2
  logging: ^1.2.0
  path_provider: ^2.1.4
  platform_provider: ^0.1.0
  html: ^0.15.4

dev_dependencies:
  database: ^0.3.1
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  chenron_mockups: ^0.0.1
  chenron: ^0.3.1
  drift: ^2.28.2
